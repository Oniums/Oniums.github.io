import { parameters, beatFrequency } from './physics.mjs';
export const chapters = [
  { title: '波与回波', subtitle: '先认识雷达发出的信号', output: '带有延迟的回波', color: 'wave' },
  { title: 'FMCW 测距', subtitle: '把距离变成一个频率', output: '与距离有关的差频', color: 'range' },
  { title: '采样与数字信号', subtitle: '把连续波形变成数字', output: '一段离散采样序列', color: 'sample' },
  { title: '从采样到距离谱', subtitle: '从数字中找到目标距离', output: '距离峰与分辨能力', color: 'spectrum' }
];
const lesson = (id, title, question, view, controls, text, observe, formula, terms, handoff, preset, task, quiz) => ({ id, title, question, view, controls, text, observe, formula, terms, handoff, preset, task, quiz });
export const lessons = [
  lesson('wave-amplitude', '1.1 一段波里有什么', '波形变高了，目标就变远了吗？', 'wave', ['amplitude'],
    ['先把电场随时间的变化画成一条曲线。横轴是时间，纵轴是相对幅度，曲线不是电波在房间里走过的轨迹。', '振幅描述振动的大小，频率描述一秒重复多少次。这两个量可以分别改变。', '这一节把频率固定。调节振幅，只观察波峰变高或变低，两个相邻波峰之间的时间不变。'],
    '上下拖动“相对振幅”，看曲线高度变化；房间里的目标距离保持不变。', 'x(t) = A · cos(2πft + φ)', 'A 是振幅，f 是频率，φ 是起始相位。纵轴采用相对单位，不把它当成真实接收功率。',
    '认识了波的大小，下一节看它重复得有多快。', { amplitude: 0.8, carrier: 24, phase: 0 }, { key: 'amplitude', prompt: '把相对振幅调到 0.4 或更低，观察周期仍保持不变。' },
    ['只减小振幅，会直接改变什么？', ['波形高度', '一个周期的时间'], 0, '振幅控制高度；周期由频率决定。']),
  lesson('wave-frequency', '1.2 频率与波长', '频率变高，波在空间里会更密还是更疏？', 'wave', ['carrier'],
    ['频率的单位 Hz 表示每秒重复次数。GHz 是十亿 Hz；图中把真实射频时间轴放大到皮秒，才能看清周期。', '波长是同一时刻，空间中两个相邻同相位位置的间距。在空气近似模型里，传播速度取光速。', '提高频率时，时间周期缩短，空间波长也缩短。它不会让电波以更高的传播速度飞出去。'],
    '从 24 GHz 调到 60 GHz，比较周期数量和下方的波长读数。', 'T = 1 / f；λ = c / f', 'T 是周期，λ 是波长；本课程取 c ≈ 3 × 10⁸ m/s。房间中的传播动画不按真实波长绘制。',
    '频率相同的两段波，也可能没有对齐；下一节看相位。', { carrier: 24, amplitude: 0.8, phase: 0 }, { key: 'frequency', prompt: '将频率调到 60 GHz，找到约 5 mm 的波长。' },
    ['从 24 GHz 提高到 60 GHz，波长会怎样？', ['变长', '变短'], 1, '传播速度近似不变，λ = c/f，所以频率越高，波长越短。']),
  lesson('wave-phase', '1.3 相位：两段波有没有对齐', '频率和振幅一样，波形为什么还能错开？', 'phase', ['phase'],
    ['相位表示周期内的位置。同样的两段余弦波，如果从周期的不同位置开始，就会在图上错开。', '青色曲线作为参考，橙色曲线改变起始相位。180° 时，参考波的波峰正好对应另一条波的波谷。', '360° 后又回到相同的波形，因此仅看一个固定频率的相位会有周期模糊。此处只解释相位，不用它单独求绝对距离。'],
    '拖到 90°、180° 和 360°，观察两条波形怎样对齐。', 'x₁(t) = A cos(2πft)；x₂(t) = A cos(2πft + φ)', 'φ 用角度显示；360° 对应完整一周。相位差改变相对位置，不改变两条波的频率。',
    '真实回波也会发生变化；下一节先看最容易理解的传播延迟。', { phase: 0, carrier: 24 }, { key: 'phase', prompt: '将相位调到 180°，让两条曲线的波峰、波谷相反。' },
    ['相位从 0° 转到 360° 后，波形会怎样？', ['回到原来的样子', '频率变成两倍'], 0, '相位每转过一整周，余弦函数就回到同一位置。']),
  lesson('wave-echo', '1.4 发出去，还要走回来', '目标距离 3 米，电波到底走了多远？', 'echo', ['distance'],
    ['把雷达看成发出信号并接收反射的同一个位置。目标在 R 米处，信号先走 R 米，再走 R 米回来。', '因此回波延迟由往返距离 2R 决定。在这里的光速近似下，3 米目标对应约 20 ns 的往返时间。', '点击播放或拖动传播进度，追踪发射、到达目标和返回三个时刻。动画是慢放示意，真实延迟以数值为准。'],
    '把小人从 3 米拖到 6 米，观察往返路径和延迟都变成两倍。', 'τ = 2R / c', 'τ 是往返延迟，R 是雷达到目标的单程距离。下方时间图使用 ns；脉冲仅用于说明传播，并非把 FMCW 画成脉冲雷达。',
    '延迟很小。接下来用扫频，把这个时间差变成更容易处理的频率差。', { distance: 3 }, { key: 'distance', prompt: '将目标移到 6 米，读出约 40 ns 的往返延迟。' },
    ['距离增加一倍，往返延迟如何变化？', ['保持不变', '增加一倍'], 1, '传播路径仍是两段 R，τ 与 R 成正比。']),
  lesson('range-chirp', '2.1 让频率沿着斜坡上升', '如果发射频率一直变化，回波会发生什么？', 'chirp', ['bandwidth', 'chirp'],
    ['一段频率随时间线性上升的信号，叫作线性调频 Chirp。这里用一条斜线画频率，而不是画射频波形。', '扫过的频率范围叫带宽 B，用时叫 T。相同时间扫过更大的带宽，斜率就更陡。', '注意区别：第 1 章的载频决定波长；这里的扫频带宽描述一次扫过多宽的频率范围。'],
    '先固定 Chirp 时长，只提高扫频带宽，再固定带宽改变时长。', 'S = B / T', 'S 是扫频斜率。图中纵轴为“相对起始频率的增量”，省略了很大的射频起始频率。',
    '有了频率斜坡，再把延迟的回波放在同一张图上。', { bandwidth: 100, chirp: 100 }, { key: 'slope', prompt: '将带宽调到 500 MHz、时长设为 100 μs，得到 5 MHz/μs 的斜率。' },
    ['固定时长，提高扫频带宽，斜率会怎样？', ['变陡', '变平'], 0, '相同时间内频率变化更大，所以 B/T 增大。']),
  lesson('range-delay', '2.2 回波是一条迟到的斜坡', '同一时刻，发射和回波的频率为什么不同？', 'chirp', ['distance'],
    ['在稳定线性段里，回波携带的是稍早时刻发出的信号，因此相对发射斜坡向右延迟 τ。', '对同一个观察时刻画一条竖线。上面的发射频率已经升高，回波频率仍是较早时刻的值。', '真实延迟只有几十纳秒，而一段 Chirp 可以长达几十微秒。全图中的两条线可能非常接近；数值读数保留真实比例。'],
    '拖动目标远离雷达，观察右侧读数里的延迟和同一时刻的频率差。', 'fTX(t) − fRX(t) = Sτ', '橙色回波按真实延迟绘制。负时间段及斜坡复位边缘没有参与本课程的采样模型。',
    '下一节通过混频，得到这个频率差对应的低频波形。', { distance: 3, bandwidth: 100, chirp: 100 }, { key: 'delay', prompt: '将目标移到 6 米，观察当前配置下差频变为 40 kHz。' },
    ['上升 Chirp 中，延迟回波的瞬时频率通常怎样？', ['高于同一时刻发射频率', '低于同一时刻发射频率'], 1, '回波来自更早的发射时刻；上升斜坡上，更早意味着更低的频率。']),
  lesson('range-mix', '2.3 混频后，留下差频', 'GHz 的射频信号，怎样变成 kHz 级的波形？', 'beat', ['distance', 'bandwidth'],
    ['混频可以理解为将发射参考信号与回波相乘。相乘后的结果包含和频与差频两部分。', '经过低通滤波，保留低频的差频分量。图中直接绘制理想混频、滤波后的信号，未模拟真实模拟电路。', '同一个静止目标，在一段理想线性 Chirp 内产生近似固定的差频。更远的目标或更陡的斜率，会让差频变大。'],
    '把目标移远，看差频曲线在相同 100 μs 时间窗里振动得更快。', 'cos(a)cos(b) = ½[cos(a−b) + cos(a+b)]；fb = Sτ', 'fb 为差频。为突出频率关系，图中归一化幅度，并省略恒定相位项、衰减与噪声。',
    '测出差频后，下一节把这个频率换算成距离。', { distance: 3, bandwidth: 100, chirp: 100 }, { key: 'beat', prompt: '固定 100 MHz / 100 μs，把目标移到 4.5 米，让差频达到 30 kHz。' },
    ['低通滤波后，本节保留哪一项？', ['差频项', '和频项'], 0, '差频较低，可以交给后面的采样与数字处理。']),
  lesson('range-distance', '2.4 从差频读出距离', '频率测出来了，怎样换回米？', 'beat', ['distance', 'chirp'],
    ['把 τ = 2R/c 代入 fb = Sτ，就能得到距离与差频的关系。只要知道扫频斜率，就可以换算。', '同一个差频值，在不同斜率下可能对应不同距离。所以解释数据时必须同时知道 Chirp 配置。', '本章假定目标静止，暂不叠加运动产生的多普勒影响。下面的频率来自理想模型；下一章开始真正对它采样。'],
    '固定目标距离，改变 Chirp 时长：差频会变化，但用对应斜率换算出的距离应保持一致。', 'R = c · fb / (2S)', '本例 100 MHz / 100 μs 的斜率为 10¹² Hz/s；20 kHz 差频对应约 3 m。',
    '本章输出：一段携带距离信息的差频信号。下一章把它变成采样序列。', { distance: 3, bandwidth: 100, chirp: 100 }, { key: 'chirp', prompt: '保持目标在 3 米，把时长改为 200 μs，观察 10 kHz 仍换算为 3 米。' },
    ['换算距离时，除了差频还必须知道什么？', ['图线的颜色', '扫频斜率'], 1, '频率与距离之间的比例由扫频斜率决定。']),
  lesson('sample-points', '3.1 给连续波形拍快照', '计算机能直接保存一条连续曲线吗？', 'samples', ['sampleRate'],
    ['连续信号在每个时刻都有数值，计算机只能记录有限个时刻的采样值。图中的圆点就是这些快照。', '相邻采样间隔 Ts = 1/fs。提高采样率会让点更密，但不会改变输入差频本身。', '这里采样的是第 2 章的低频差频信号，不是直接以这点采样率记录 GHz 射频。'],
    '载入实验后，在 80、160、320 kS/s 之间切换，比较相同时间窗中的采样点。', 'tₙ = n/fs；x[n] = x(tₙ)', 'fs 是每秒采样次数；kS/s 表示每秒一千个样本。本节先显示理想采样值，量化在 3.3 单独引入。',
    '点多或点少不只是画面密度问题；采样太慢可能看见错误的频率。', { distance: 3, bandwidth: 100, chirp: 100, sampleRate: 160, fraction: 1 }, { key: 'samples', prompt: '在 100 μs 采样窗内选择 320 kS/s，获得 32 个采样点。' },
    ['固定采样时间窗，提高采样率会怎样？', ['记录更多采样点', '让原信号频率变高'], 0, '采样改变记录密度，不改变原来的连续信号。']),
  lesson('sample-alias', '3.2 点太少，会看错什么', '同一组采样点，会不会属于另一条波？', 'alias', ['sampleRate', 'distance'],
    ['当采样率不足时，高频信号与较低频的某条波，可能在所有采样时刻取到相同数值。这就是混叠。', '图中青色为真实差频，橙色为折叠到 0～fs/2 的余弦波。发生混叠时，两条曲线不同，但采样点仍重合。', '对本课的实数低通信号，要让最高差频低于 fs/2。实际硬件还需要抗混叠滤波和余量，刚好踩在边界上并不可靠。'],
    '载入 20 kHz 信号 / 32 kS/s 示例，看看它为什么也像 12 kHz；再提高采样率解除混叠。', '需满足 fmax < fs/2；本例 20 kHz → |20 − 32| = 12 kHz', '橙色曲线用于展示采样歧义，不是已经证明原信号就是低频。此处省略量化，以便采样点精确对齐。',
    '时间方向的采样讲清了，下一节看幅度方向怎样变成有限个数。', { distance: 3, bandwidth: 100, chirp: 100, sampleRate: 32, fraction: 1 }, { key: 'no-alias', prompt: '保持目标在 3 米，把采样率提高到 80 kS/s 或更高，使 20 kHz 低于 fs/2。' },
    ['对已经混叠的一段数据，只增加 FFT 点数能恢复原频率吗？', ['不能', '一定能'], 0, '原始采样已经存在歧义，后处理补点不能凭空找回信息。']),
  lesson('sample-quantization', '3.3 幅度也要落在格子里', '采样值为什么不能想记多精细就多精细？', 'quantize', ['bits', 'amplitude'],
    ['采样决定何时记录，量化决定幅度可以记录成哪些数字。它们是两个不同的过程。', '本节采用归一化 −1～1 范围的理想均匀量化器，b 位提供 2ᵇ 个刻度。橙色点为量化后的值。', '提高位数会减小刻度间距。真实 ADC 的有效位数、噪声和失真还会影响结果，不能只凭标称位数判断性能。'],
    '在 2 位和 8 位之间切换，看橙色点到青色理想点的误差。', '等级数 = 2ᵇ；本模型步长 Δ = 2/(2ᵇ−1)', '这是用于教学的端点对称量化模型。不同硬件的满量程、编码和量化边界可能不同。',
    '一帧不只有采样率和位数，还需要决定连续观察多久。', { distance: 3, bandwidth: 100, chirp: 100, sampleRate: 160, amplitude: 0.8, bits: 2, fraction: 1 }, { key: 'bits', prompt: '切换到 8 位或更多，观察相同采样时刻的量化误差缩小。' },
    ['提高 ADC 位数，直接改善的是哪一个方向？', ['幅度刻度精细度', '采样时刻的密度'], 0, '位数控制幅度等级；时间间隔由采样率控制。']),
  lesson('sample-record', '3.4 这一段，究竟采了多久', '同样是 128 个点，它们一定携带相同的信息吗？', 'samples', ['fraction', 'sampleRate'],
    ['一段数据由采样率 fs 和采样点数 N 共同描述。通常用 N/fs 表示数字处理中的观测窗长度。', '最后一个样本位于 (N−1)/fs；它与用于频谱刻度的 N/fs 是相关但不同的两个量。', '固定采样率而缩短采样窗，会减少有效观测数据。下一章会看到，观察时长也会影响区分相近频率的能力。'],
    '在完整、半段、四分之一段之间切换，看采样点数和有效扫频带宽同步变化。', 'Tobs = N/fs；Beff = S · Tobs', 'Beff 是实际采样窗覆盖的扫频带宽。样本数向下取整，读数使用实际 N/fs。',
    '本章输出：采样率已知的一段数字序列。下一章从这些数里找出频率。', { sampleRate: 1280, chirp: 100, bandwidth: 100, fraction: 1 }, { key: 'record', prompt: '选择 1280 kS/s 和半段采样窗，观察 64 点与 50 μs。' },
    ['固定采样率，把有效采样窗缩短一半，会怎样？', ['有效采样点约减半', '真实带宽自动增加一倍'], 0, '观测时长减少，点数与覆盖的有效扫频带宽也随之减少。']),
  lesson('spectrum-components', '4.1 两个目标，叠成一条波', '看起来复杂的波形，能不能拆成几种频率？', 'components', ['separation', 'secondAmplitude'],
    ['接收信号可以包含多个目标的回波。理想线性模型中，每个静止目标贡献一个对应距离的差频分量。', '图中分别画出目标 A、目标 B 的分量，再画出两者相加的波形。相加后的波形通常不再像单个余弦。', '频谱分析要回答：这段数据中，各种频率成分分别有多强。FFT 是高效计算离散傅里叶变换的一种算法。'],
    '拖动第二个目标，观察它自己的波形和合成波形变化；目标 A 的距离不变。', 'x[n] = A₁cos(2πf₁n/fs) + A₂cos(2πf₂n/fs + φ₂)', '两个目标的相对幅度和相位采用构造值；未模拟真实反射面积、距离衰减和遮挡。',
    '接下来把合成信号送进 FFT，并为频谱横轴换上距离单位。', { distance: 3, separation: 1.5, bandwidth: 300, chirp: 100, sampleRate: 1280, fraction: 1, amplitude: 0.8, secondAmplitude: 0.65, window: 'rect' }, { key: 'two-targets', prompt: '把目标间距调到 1 米或更大，观察两个分量的频率不同。' },
    ['两个不同距离的静止目标，通常贡献什么？', ['两个差频分量', '自动合成一个目标距离'], 0, '每个目标都对应自己的传播延迟，在线性模型中形成不同差频。']),
  lesson('spectrum-range', '4.2 给频谱横轴换上“米”', 'FFT 输出的频率格点，怎样对应到目标距离？', 'spectrum', ['distance'],
    ['上图保留实际采样序列，下图对这些样本计算 FFT，显示各频率的幅度。频谱来自当前采样数据，并非在目标位置画一个预设峰。', '第 k 个频率格点为 k·fs/NFFT，再用第 2 章的 R = cfb/(2S) 换成米。', '目标不一定落在格点正中，峰值会受到有限观测窗和窗函数影响。图中的虚线表示设定的真实距离，曲线表示计算结果。'],
    '缓慢拖动目标，比较真实距离虚线与频谱峰位置。', 'fₖ = kfs/NFFT；Rₖ = c fₖ/(2S)', 'N 是真实采样点数，NFFT 是变换长度；如果补零，两者可以不同。图中仅展示实信号的非负频率部分。',
    '能定位一个峰还不够；下一节看两个相近目标能否分开。', { distance: 3, bandwidth: 300, chirp: 100, sampleRate: 1280, fraction: 1, padding: 4, window: 'rect' }, { key: 'range-peak', prompt: '将目标移到 5 米，观察距离谱峰随之移动到附近。' },
    ['把频率轴换成距离轴，需要用到哪一项？', ['扫频斜率', '曲线显示宽度'], 0, 'R = cf/(2S)，坐标换算依赖真实采样与扫频配置。']),
  lesson('spectrum-resolution', '4.3 两个目标，什么时候分不开', '图上两个小人，为什么只看到一个宽峰？', 'spectrum', ['bandwidth', 'separation'],
    ['有限观测让单个频率在频谱中表现为有宽度的主瓣。两个目标太近时，两组响应会重叠。', '理想矩形观测窗下，常用 ΔR ≈ c/(2Beff) 作为距离分辨尺度。增加有效扫频带宽，可以缩小这一尺度。', '实际能否分辨还取决于目标强弱、相位、窗函数和噪声。这里只给分辨尺度作参考，不把它当成两个目标一定可分的硬开关。'],
    '载入间距 0.4 米的两个目标，从 100 MHz 逐步调到 1000 MHz，观察主瓣宽度和两个峰。', 'ΔR ≈ c/(2Beff)；Beff = S · N/fs', '这里关注区分相邻目标的能力，它与单个目标距离估计的精度不是同一个概念。',
    '最后看两种常用操作：加窗和补零。它们分别改变什么？', { distance: 3, separation: 0.4, bandwidth: 100, chirp: 100, sampleRate: 1280, fraction: 1, padding: 4, window: 'rect', amplitude: 0.8, secondAmplitude: 0.65 }, { key: 'resolution', prompt: '保持间距 0.4 米，将带宽提高到 1000 MHz，让理想分辨尺度降到约 0.15 米。' },
    ['完整采样条件下，提高扫频带宽主要有助于什么？', ['区分更近的两个目标', '让光传播得更快'], 0, '有效带宽更宽，有限观测对应的距离主瓣尺度更小。']),
  lesson('spectrum-window', '4.4 加窗与补零，各自改变什么', '曲线更平滑，就代表雷达看得更清楚了吗？', 'spectrum', ['window', 'padding', 'secondAmplitude'],
    ['直接截取一段数据相当于矩形窗，目标未对齐频率格点时会出现谱泄漏。Hann 窗降低边缘权重，通常减小旁瓣，但也会加宽主瓣。', '弱目标旁边有强目标时，降低旁瓣可能有帮助；两个目标非常接近时，更宽的主瓣又可能不利。需要结合场景比较。', '补零是在已有样本后追加零，得到更密的频谱计算格点。它能让峰形更容易观察，但不会增加真实观测时长，也不会改善基本分辨尺度。'],
    '先固定窗函数，把补零从 1× 调到 8×，观察格点间距变化而 ΔR 不变；再切换 Hann 窗比较主瓣与旁瓣。', 'Δfgrid = fs/NFFT；ΔR尺度 ≈ c/(2Beff)', 'FFT 幅度按窗的相干增益归一化；本节未加入随机噪声。补零倍率相对于覆盖原样本的最小 2 次幂长度。',
    '你已经走完：目标 → 延迟 → 差频 → 采样 → 距离谱。后续将继续讲测速、测角、检测与有人判定。', { distance: 3.13, separation: 1.3, bandwidth: 500, chirp: 100, sampleRate: 1280, fraction: 1, padding: 1, window: 'rect', amplitude: 0.8, secondAmplitude: 0.08 }, { key: 'padding', prompt: '将补零设为 8×，确认真实采样点数和理想分辨尺度不变，再切换 Hann 窗。' },
    ['补零让频谱格点更密，它新增真实测量信息了吗？', ['没有', '新增了更多有效采样'], 0, '零不是新测量；观测时长与有效扫频带宽仍由原始样本决定。'])
];
export function taskPassed(key, s) {
  const p = parameters(s);
  const noAlias = s.sampleRate * 1000 > 2 * beatFrequency(s.distance + s.separation, s.bandwidth, s.chirp);
  switch (key) {
    case 'amplitude': return s.amplitude <= 0.4;
    case 'frequency': return s.carrier === 60;
    case 'phase': return s.phase === 180;
    case 'distance': return Math.abs(s.distance - 6) < 0.06;
    case 'delay': return Math.abs(s.distance - 6) < 0.06 && s.bandwidth === 100 && s.chirp === 100;
    case 'slope': return s.bandwidth === 500 && s.chirp === 100;
    case 'beat': return Math.abs(s.distance - 4.5) < 0.06 && s.bandwidth === 100 && s.chirp === 100;
    case 'chirp': return s.chirp === 200 && s.bandwidth === 100 && Math.abs(s.distance - 3) < 0.06;
    case 'samples': return s.sampleRate === 320 && s.chirp === 100 && s.fraction === 1;
    case 'no-alias': return s.sampleRate >= 80 && Math.abs(s.distance - 3) < 0.06 && s.bandwidth === 100 && s.chirp === 100;
    case 'bits': return s.bits >= 8;
    case 'record': return s.sampleRate === 1280 && s.fraction === 0.5 && s.chirp === 100;
    case 'two-targets': return s.separation >= 1;
    case 'range-peak': return Math.abs(s.distance - 5) < 0.06 && p.count >= 2 && p.frequency < p.fs / 2;
    case 'resolution': return s.bandwidth === 1000 && Math.abs(s.separation - 0.4) < 0.01 && p.resolution <= 0.151 && noAlias;
    case 'padding': return s.padding === 8 && s.window === 'hann' && p.count >= 2 && noAlias;
    default: return false;
  }
}
